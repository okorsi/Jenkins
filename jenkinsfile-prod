if ("${env.JOB_NAME}".contains('PROD')) {
    BUILD = "Production"
} else {
    BUILD = "Something else"
}
pipeline {
    agent any
stages { 
        stage('2-Deploy') {
            steps {
                echo "Start of Stage Deploy..."
                echo "Deploying......."
                sh "ls -la"
                echo "$BUILD"
                sh '''
                   echo "Line1"
                   echo "Line2"
                '''
                echo "End of Stage Build..."
            }
        }
        stage('3-Celebrate') {
            steps {
                echo "Slava Ukraine!"
            }
        }
    }
  }
